# 보안회사 웹사이트 (Plan 기반 제작) — 실습 가이드

방금 인라인 채팅으로 보안회사 웹사이트를 만들어봤습니다. 이번엔 **똑같은 사이트**를 Copilot의 **Plan 모드**로 다시 만들어봅니다. 이번엔 코드를 바로 만들지 않고, 먼저 "계획서"를 세운 뒤 그 계획을 통째로 실행에 넘깁니다.

## 0️⃣ 시작하기 전에

- 이 저장소를 **Fork** → 로컬에 **Clone**
- **`dev` 브랜치를 만들고 그 위에서 작업합니다** (지난주와 동일한 3브랜치 전략)
  ```
  git checkout -b dev
  ```
- 방금 만든 인라인 결과물의 배포 URL을 옆에 띄워두세요 (나중에 비교할 것입니다)

> 💡 요구사항은 방금 만든 것과 **완전히 동일**합니다. 같은 조건에서 방식만 다르게 진행해야 제대로 비교할 수 있습니다.
>
> 🌿 **브랜치 전략 (지난주 복습)**
> - `main`: 원본 그대로 보존
> - `dev`: 오늘 실습 작업은 전부 여기서 (Agent 실행도 이 브랜치 위에서)
> - `gh-pages`: 완성 후 배포용 (dev를 merge)

## 1️⃣ 오늘 만드는 것 (지난 실습과 동일)

```
nav (앵커링크: Hero / Services / About / Contact)
 └─ hero (회사 슬로건 + 버튼)
 └─ services (서비스 카드 3개, 각 카드에 "자세히 보기" 링크)
 └─ about (회사 소개)
 └─ contact/footer (연락처)
```

## 2️⃣ Plan 모드가 뭔가요?

- **Plan 모드**: 코드를 전혀 건드리지 않고, "무엇을 어떤 순서로 만들지" 계획서만 세워주는 모드입니다. 읽기 전용으로 동작해서, 이 단계에서는 어떤 파일도 바뀌지 않습니다.
- **Agent 모드**: 계획을 실제로 실행하는 모드입니다. 파일을 직접 만들고 수정합니다.
- **연결 방법**: Plan에서 세운 계획이 마음에 들면 **"Start Implementation"** 버튼 하나로 Agent에게 실행을 넘깁니다. 둘은 별개의 도구가 아니라 하나의 흐름입니다.

```
[Plan] 계획 세우기 (코드 변경 없음)
    ↓ "Start Implementation" 클릭
[Agent] 계획대로 파일 생성·수정 (실행)
```

## 3️⃣ STEP 1 — Plan 모드로 계획 세우기

Copilot Chat에서 모드를 **Plan**으로 선택하고, 요구사항을 입력합니다.

**프롬프트 예시**
```
보안회사 원페이지 웹사이트를 만들려고 해.
파일은 index.html, style.css, script.js로 나눠서 써.
구조는 다음과 같아:
- nav: 앵커링크(Hero, Services, About, Contact)
- hero: 슬로건 + 버튼
- services: 서비스 카드 3개(취약점 진단, 모의해킹, 보안 컨설팅), 각각 "자세히 보기" 링크
- about: 회사 소개
- contact/footer: 연락처(예시 값)
- 모바일에서 nav가 햄버거 메뉴로 토글되는 기능 포함

전체 구현 계획을 세워줘.
```

- [ ] Plan이 만든 계획서를 처음부터 끝까지 읽어보기
- [ ] 계획에 5개 섹션이 다 포함됐는지 확인
- [ ] 파일 구조(`index.html`/`style.css`/`script.js`)가 계획에 명시됐는지 확인

**계획이 아쉬우면 채팅으로 보완 요청**해보세요. 이 단계에서는 코드가 아니라 계획만 바뀝니다.

```
서비스 카드에 hover 효과도 계획에 추가해줘.
```

## 4️⃣ STEP 2 — Start Implementation으로 실행

계획이 만족스러우면 **"Start Implementation"** 버튼을 클릭합니다.

- Copilot이 Agent 모드로 전환되면서 계획을 단계별로 실행합니다.
- 파일이 만들어지고 수정되는 과정을 화면으로 지켜보세요.
- 중간에 원하는 것과 다르게 진행되면 그 자리에서 바로 추가 지시를 할 수 있습니다 (완전히 알아서 끝까지 진행되는 게 아닙니다).

- [ ] Agent가 `index.html`, `style.css`, `script.js`를 모두 생성했는지 확인
- [ ] 브라우저에서 결과 확인

## 5️⃣ 결과 비교

인라인으로 만든 것과 Plan으로 만든 것을 나란히 열어놓고 비교해봅니다.

| 비교 항목 | 인라인(지난 실습) | Plan 기반(이번 실습) |
|---|---|---|
| 완성까지 걸린 체감 시간 | | |
| 중간에 "이게 아닌데" 하고 되돌아간 횟수 | | |
| 섹션 누락이나 빠뜨린 부분 | | |
| 전체적인 완성도 | | |

- [ ] 위 표를 직접 채워보고, 어떤 점이 다르게 느껴졌는지 한두 줄로 적어보기

## 6️⃣ (시간이 남으면) 서브페이지 도전

services의 "자세히 보기" 링크를 별도 페이지로 연결하는 것도 Plan에게 요청해볼 수 있습니다.

```
services 카드의 "자세히 보기" 링크가 각각 
별도의 상세 페이지로 연결되도록 계획을 추가해줘.
```

## 7️⃣ 배포 (GitHub Pages)

지난주, 그리고 방금 인라인 실습과 동일한 방식으로 배포합니다.

1. `dev` 브랜치에서 작업 내용을 커밋 (Agent가 만든 결과물도 `dev` 위에서 커밋)
2. `gh-pages` 브랜치를 만들고 `dev`를 merge
   ```
   git checkout -b gh-pages
   git merge dev
   git push origin gh-pages
   ```
3. 저장소 **Settings → Pages**에서 배포 브랜치를 `gh-pages`로 확인
4. 배포된 URL 접속해서 정상 동작 확인

## 8️⃣ 제출 (Pull Request)

1. 본인 저장소에서 `dev` 브랜치로 이동한 뒤 **Contribute** → **Open pull request**
2. 원본 저장소(`hitech26-w4-web-plan`)의 `main` 브랜치로, **`dev` 브랜치 기준**으로 PR
3. 제목에 이름 포함 (예: `[홍길동] 보안회사 웹사이트(Plan) 제출`)

> 💡 이 PR은 실제로 Merge되지 않는 제출용입니다.

## ✅ 오늘 완성 체크

- [ ] `dev` 브랜치를 만들고 그 위에서 작업했다
- [ ] Plan 모드로 계획을 세우고, 코드가 바뀌지 않는다는 것을 직접 확인했다
- [ ] "Start Implementation"으로 Agent 실행까지 이어지는 흐름을 경험했다
- [ ] 5개 섹션이 모두 있는 결과물을 완성했다
- [ ] 인라인 결과물과 비교 표를 채웠다
- [ ] GitHub Pages로 배포하고 링크 접속을 확인했다
- [ ] `dev` 브랜치 기준으로 조직 저장소에 PR을 제출했다

## ❓ 자주 묻는 질문 FAQ

### Q1. Plan 모드에서 코드가 하나도 안 바뀌는 게 맞나요?
**A**: 네, 맞습니다. Plan 모드는 "계획서"만 작성하는 읽기 전용 단계입니다. 파일이 바뀌는 건 "Start Implementation"을 누른 이후, Agent 모드로 넘어갔을 때부터입니다.

### Q2. Start Implementation을 눌렀는데 계획과 다르게 실행돼요
**A**: Agent가 계획을 따라가다가 판단이 필요한 지점에서 조금씩 다르게 갈 수 있습니다. 이상하게 진행되면 채팅으로 "계획대로 ~부분을 다시 해줘"처럼 바로 지시하면 됩니다.

### Q3. 인라인보다 Plan이 항상 더 빠른가요?
**A**: 꼭 그렇지는 않습니다. 계획을 세우는 시간이 추가로 들어가기 때문에, 전체 시간보다는 "완성 후 되돌아가서 고치는 횟수"가 줄어드는 쪽에 더 주목해보세요.

---

**📚 수고하셨습니다! 다음 주에는 게임 제작을 통해 Agent 모드를 본격적으로 더 써보고, PRD(제품 요구사항 문서) 작성법도 배웁니다.**